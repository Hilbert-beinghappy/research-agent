# @research-agent/contracts

Versioned TypeScript and TypeBox contracts for Pi Research Agent projects, results, adapters, exchange bundles, collaboration changes, model-route decisions, and the SDK/RPC v1 inspection protocol.

The package contains data contracts only. It performs no network access, filesystem writes, model calls, or adapter execution.
