# @research-agent/contracts

Versioned TypeScript and TypeBox contracts for Pi Research Agent projects, results, adapters, exchange bundles, collaboration changes, and model-route decisions.

The package contains data contracts only. It performs no network access, filesystem writes, model calls, or adapter execution.
