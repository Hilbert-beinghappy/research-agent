# Open Catalog Adapter example

This data-only fixture demonstrates Source Adapter contract v1 and the JSONL process protocol. It performs no network access and imports no Research Agent implementation module.

Run from the `pi-research-agent` package:

```sh
npm run conform:adapter -- examples/adapters/open-catalog
npm run conform:adapter -- examples/adapters/open-catalog --strong
```
