# JSON Artifact Adapter example

This independent fixture demonstrates Artifact Adapter contract v1 and the JSONL process protocol. It writes only to the host-provided staging directory and imports no Research Agent implementation module.

Run from the `pi-research-agent` package:

```sh
npm run conform:adapter -- examples/adapters/json-artifact
```
