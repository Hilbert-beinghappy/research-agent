# Reproducible Runtime Adapter example

This independent fixture demonstrates Analysis Runtime Adapter contract v1 and the JSONL process protocol. It imports no Research Agent implementation module and performs no external work.

Run from the `pi-research-agent` package:

```sh
npm run conform:adapter -- examples/adapters/repro-runtime
```
