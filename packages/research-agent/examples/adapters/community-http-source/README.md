# Community HTTP Source Adapter

This Apache-2.0 example implements Source Adapter contract v1 without changing Research Agent core code. Its conformance fixture is offline. A normal search emits an `http` broker request; the Host decides whether to approve and perform that request. The Adapter never receives credentials or direct project write access.
