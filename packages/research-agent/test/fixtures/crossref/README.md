# Crossref fixture provenance

These files are synthetic, minimal responses shaped after the public Crossref REST API v1 contract, including the notice-to-work direction of `update-to`. They contain no copied publication abstracts, real credentials, or live API responses and are used only for offline adapter tests.
