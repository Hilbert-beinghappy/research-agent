# Dedup gold fixture

This directory contains synthetic bibliographic metadata only. `gold.json` separates safe automatic merges from review-only duplicate candidates; weak-key review cases are deliberately ambiguous.
