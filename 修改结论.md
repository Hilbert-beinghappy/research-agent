# 1. 最终结论

审计对象固定为 `main@72e83b547d9a236757c9aa88777b1734a00f4401`。本次审计日期为 **2026-08-07**。

## 发布裁决

| 使用场景                                          | 裁决               | 结论                                                         |
| ------------------------------------------------- | ------------------ | ------------------------------------------------------------ |
| **以“Doro Research Agent v2.0.0 稳定版”公开发布** | **NO-GO**          | 代码不是骨架，核心科研状态、证据、分析、适配器和 SDK/RPC 均有真实实现；但固定提交没有获得完整 CI 资格，同时存在模型数据外传策略绕过、证据生成来源错误标注、已知高危 PDF 依赖三项发布阻断问题。 |
| **仓库作者个人日常科研使用**                      | **CONDITIONAL GO** | 限于单用户、单进程、主动备份、可信 PDF、非敏感项目或本地模型，并且所有证据—论断关系和投稿产物由人复核。不得依赖当前 `modelEgressAllowed=false` 作为完整保密边界，也不得把 Stata、真实 Zotero 同步和第三方安装视为已验证能力。 |
| **第三方安装并作为稳定科研工具使用**              | **NO-GO**          | README 明确说明尚未发布公共 npm 包；没有独立 `doro` 启动命令；固定提交未完成发布 CI；公共导出仍直接指向 `src/*.ts`；安全策略和安装资格尚未闭合。 |

## 总体推荐方案

**不重写现有架构。将当前提交重新定性为 `v2.0.0 release candidate / source preview`，冻结功能扩张，用两周完成一次 P0 安全与发布硬化，然后发布 `v2.0.1`。**

这套方案只有一个方向：

> 保留 Pi Core、单主 Agent、11 Skills、14 个聚合工具、文件化 `.research` 状态和公共 contracts；修复模型—数据边界、证据来源标记、PDF 漏洞、CI 资格和并发持久化，不另建数据库、UI、SaaS 或多 Agent 框架。

### 最重要的总判断

**代码事实：** 该项目不是“README 已到 v2.0、实现仍停留在骨架”。`packages/research-agent` 包含实质性的状态事务、PDF 解析、证据验证、分析运行器、适配器子进程协议、SDK/RPC、迁移恢复和发布扫描；`packages/research-agent-contracts` 也包含真实 schema、validators 和 adapter/RPC 协议。研究包版本是 `2.0.0`，Pi coding-agent 及内部 Pi 包是 `0.83.0`。根 monorepo 的版本仍是 `0.0.3`，属于仓库元数据和产品版本分层，不代表 Pi 仍停留在旧版本。参见 [research-agent/package.json L1–120](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/package.json#L1-L120)、[coding-agent/package.json L1–90](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/coding-agent/package.json#L1-L90) 和 [根 package.json L1–40](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/package.json#L1-L40)。

**审稿结论：** 当前主要问题不是“没有实现”，而是**安全边界、审计语义和固定提交发布资格没有达到实现规模所要求的标准**。

------

# 2. 架构事实图

## 2.1 真实调用与数据流

```text
用户
  │
  ▼
Pi 0.83.0 Terminal / Session / Model Loop
  │
  │ 加载 research-agent extension 与 Skills
  ▼
packages/research-agent
  ├─ 11 个 Skills
  │    └─ 任务规划、模型判断、工具使用说明
  │
  ├─ 14 个聚合 Research Tools
  │    ├─ search / import / documents / query
  │    ├─ evidence / citations / artifacts / knowledge
  │    ├─ monitor / design / analysis
  │    ├─ qualitative / case study / manuscript / review
  │    └─ 将模型参数交给确定性业务模块
  │
  ├─ 确定性核心
  │    ├─ contracts schema + validators
  │    ├─ 路径和权限检查
  │    ├─ 来源、文档、证据、论断、引文状态机
  │    ├─ revision / hash / transaction / journal
  │    └─ artifact / operation / provenance 记录
  │
  ├─ 外部运行路径
  │    ├─ source adapters：Crossref / OpenAlex 等
  │    ├─ document parsers：PDF 等
  │    ├─ analysis runtimes：Python / R / Stata
  │    └─ export adapters：Zotero / Markdown vault 等
  │
  ▼
.research 规范化项目状态
  ├─ manifest 与版本
  ├─ typed canonical records
  ├─ operation / provenance / approval
  ├─ artifacts / runs / caches
  └─ staged transaction + journal + rollback

旁路只读接口：
第三方进程 ──stdin/stdout JSON-RPC──> research-agent-rpc
                                   └─ 7 个只读 SDK/RPC 方法
```

## 2.2 组件职责判定

| 组件                                    | 代码事实                                                     | 状态                                |
| --------------------------------------- | ------------------------------------------------------------ | ----------------------------------- |
| **Pi Core / Pi Session**                | Pi 包固定在 `0.83.0`；负责模型会话、通用工具循环、终端和 extension 加载。Doro 提交主要增加启动品牌头，没有新增一套会话框架。 | **已实现，沿用上游**                |
| **Research Package**                    | `pi-research-agent@2.0.0` 注册科研工具、状态服务、适配器和分析路径。 | **已实现**                          |
| **Skills**                              | 仓库中有 11 个 Skill 目录；它们主要规定科研流程、门禁和工具调用方法，不应直接成为 canonical writer。 | **已实现**                          |
| **14 个聚合 Tools**                     | `registerResearchTools()` 注册搜索、文档、查询、证据、分析、质性、案例、写作等聚合工具。聚合设计减少模型工具选择面，但实现集中在约 175 KB 的单文件中。 | **已实现，但内部过度集中**          |
| **Contracts**                           | 独立包提供 schema 1.5.0、validators、adapter protocol 1.1.0 和 SDK/RPC protocol 1.0.0。 | **已实现**                          |
| **`.research` 状态**                    | canonical records 通过 revision、hash、transaction journal、staging、backup 和 manifest commit 管理；旧 schema 只读打开并要求迁移。 | **已实现；多进程并发部分实现**      |
| **证据层级**                            | metadata、abstract、unlocated full text、located full text 等在 schema 和 validator 中分层。精确摘录会对当前解析产物进行检查。 | **已实现**                          |
| **table/figure、dataset/appendix 证据** | schema 预留了等级，但当前 commit 路径明确返回 unsupported。  | **仅契约预留**                      |
| **Python / R / Stata**                  | 运行器会真实 spawn 外部程序，暂存脚本与输入、限制超时和输出、记录哈希与版本。Python/R 有前序 CI 资格记录；Stata 只有在配置 `STATA_BIN` 时运行。 | **Python/R 已实现；Stata 部分验证** |
| **Zotero**                              | 有 Zotero/Better BibTeX 格式生成和知识导出；这不等于已验证的桌面 Zotero 双向实时同步。 | **部分实现**                        |
| **Obsidian**                            | 主要表现为 portable Markdown/Obsidian artifact 或 vault export，不是独立 Obsidian 插件。 | **部分实现**                        |
| **SDK/RPC**                             | 七个只读方法有 schema、实现和 stdin/stdout JSON-RPC server。 | **已实现；第三方发布资格不足**      |
| **Doro 独立入口**                       | research 包只有 `research-agent-rpc` bin；常规启动仍是 Pi 加载 research extension。commit 增加了 Doro 品牌头，但没有 `doro` CLI。 | **品牌已实现，独立入口未实现**      |
| **公共 npm 安装**                       | README 明确说明包尚未公开发布。                              | **仅规划**                          |

组件证据可见 [README L1–220](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/README.md#L1-L220)、[tools.ts L4740–5150](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/extension/tools.ts#L4740-L5150)、[contracts schemas.ts L600–1060](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent-contracts/src/schemas.ts#L600-L1060)、[transactions.ts L1–520](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/project/transactions.ts#L1-L520) 和 [runtime.ts L1–520](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/analysis/runtime.ts#L1-L520)。

## 2.3 模型与确定性代码的边界

### 确定性代码执行的部分

- 路径规范化、项目边界和 symlink escape 检查；
- record schema、revision、hash 和 manifest 一致性；
- PDF 当前文件和当前解析产物的绑定；
- located evidence 的 locator、excerpt 与解析文本检查；
- 权限 action class、审批 fingerprint、成本和目标绑定；
- canonical record 写入、journal、staging、backup、commit 与 rollback；
- 分析进程的超时、输出上限、输入输出哈希和 allowlist 导入；
- adapter JSONL 协议、消息大小、超时、签名和权限声明。

参见 [validators.ts L120–300](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent-contracts/src/validators.ts#L120-L300)、[evidence/commit.ts L1–620](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/evidence/commit.ts#L1-L620)、[security/policy.ts L1–260](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/security/policy.ts#L1-L260) 和 [adapters/runner.ts L1–520](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/adapters/runner.ts#L1-L520)。

### 交给模型判断的部分

- 检索意图和查询计划；
- 证据卡的 paraphrase、evidence statement；
- claim 文本；
- evidence—claim 的 supports/refutes/contextualizes 关系及理由；
- 研究设计、质性编码、案例解释；
- 论文段落、审稿意见和修订建议。

### 关键边界结论

**代码事实：** 模型不能伪造一个不存在于当前解析文本中的 exact located excerpt；validator 会检查文档、解析产物、locator 和 exact snippet。

**代码事实：** 模型仍可以生成“这个摘录支持某论断”的语义判断。当前代码不能确定性证明 entailment。

**阻断性缺陷：** 模型暴露的 `EvidenceDraftSchema` 允许调用方把 extraction method 选成 `deterministic` 或 `imported`，随后 `commitEvidence()` 又把 `modelProvider`、`modelId`、`promptHash` 全部写成 `null`。因此，模型生成的 paraphrase、evidence statement 和 claim-link rationale 可以进入 canonical state，却被记为非模型来源。参见 [tools.ts L292–336](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/extension/tools.ts#L292-L336) 和 [tools.ts L2725–2748](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/extension/tools.ts#L2725-L2748)。

------

# 3. 宣称—实现—测试对照表

| 产品宣称                         | 实现位置                                                     | 测试证据                                                     | 判定                                      |
| -------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ----------------------------------------- |
| 11 个科研 Skills、14 个聚合工具  | `packages/research-agent/skills/`；`registerResearchTools()` | Skill 目录和工具注册均存在；前序 focused CI 执行 deterministic/e2e tests | **SUPPORTED**                             |
| 文献检索、规范化、去重           | source adapters、source records、dedup、monitor              | Scenario A 和 adapter tests 为合成/fixture 路径；未在固定提交独立连接真实服务 | **PARTIAL**                               |
| 全文状态管理                     | `DocumentRecord` 区分 acquired/parsed/failed，绑定本地文件和解析输出 | validator 和 Scenario A 覆盖；真实恶意 PDF 未验证            | **SUPPORTED**                             |
| 元数据、摘要、全文严格区分       | contracts evidence levels、document state、commit validator  | located/unlocated、metadata/abstract 分支有测试              | **SUPPORTED**                             |
| 精读、证据卡、证据矩阵           | evidence commit/query/artifact                               | exact excerpt 有确定性测试；语义性字段来源标记存在缺陷       | **PARTIAL**                               |
| 引文核验与来源追踪               | citation verification records、provider raw results、field checks、expiry | 合成 provider/fixture tests；真实服务变化和撤稿边界未独立复跑 | **PARTIAL**                               |
| 研究设计                         | design records、confirmation、research_design tool           | 有接口和确定性状态测试                                       | **SUPPORTED**                             |
| Python 定量分析                  | 真实 subprocess runner、qualification script                 | 前序 Ubuntu/macOS CI observed pass；固定提交未独立运行       | **PARTIAL**                               |
| R 定量分析                       | 同上                                                         | 前序 Ubuntu/macOS CI observed pass；固定提交未独立运行       | **PARTIAL**                               |
| Stata 分析                       | 同一 runtime 可解析 `STATA_BIN` 并执行                       | CI 中没有配置 Stata；本次未运行                              | **PARTIAL**                               |
| 质性编码                         | qualitative records、model suggestion 与确认路径             | 主要依赖合成数据和模型 stub；真实访谈/编码一致性未验证       | **PARTIAL**                               |
| 案例比较                         | case records、case-study tool                                | 有结构和 fixture tests；真实案例研究工作流未验证             | **PARTIAL**                               |
| 论文写作、审稿、修订             | manuscript、submission gate、review findings、revision decision | 有 deterministic gate 和 scenario tests；真实模型端到端未验证 | **PARTIAL**                               |
| Zotero 适配                      | Better BibTeX/CSL JSON 生成、profile/export、push contract   | 格式和适配器测试存在；真实 Zotero 桌面端/库同步未验证        | **PARTIAL**                               |
| Obsidian 适配                    | Obsidian/Markdown artifact export                            | 可生成便携产物；不是已验证的 Obsidian 插件或双向同步         | **PARTIAL**                               |
| SDK/RPC 第三方扩展               | contracts 中七个只读方法；stdio server 和 bin                | schema/handler tests 存在；没有固定提交的 clean third-party install | **PARTIAL**                               |
| 文件式恢复、迁移和复现           | journal/staging/backup/hash/migration/Scenario D             | 合成 kill-point 和 migration fixture 覆盖较强                | **SUPPORTED**，但多进程写入仅 **PARTIAL** |
| 本地优先、无数据库               | canonical state 为文件，RPC 为本地 stdio                     | 代码直接支持                                                 | **SUPPORTED**                             |
| 敏感数据可由 policy 禁止模型外传 | manifest 有 `modelEgressAllowed`                             | `research_query_corpus` 路径未执行该策略                     | **UNSUPPORTED**                           |
| Doro 独立产品入口                | session header 品牌化                                        | commit 有品牌测试；无 `doro` bin                             | **PARTIAL**                               |
| 第三方直接安装稳定 v2.0          | README 源码启动；包未公开 npm                                | 无固定提交 clean install 证据                                | **UNSUPPORTED**                           |

前序成功的 Research Agent workflow 是 [Actions run 31152618515](https://github.com/Hilbert-beinghappy/research-agent/actions/runs/31152618515)，对应的是较早提交 `db0c23e…`，不能替代固定提交 `72e83b5…` 的发布资格。该运行在 Ubuntu、Windows、macOS 执行了 package check、deterministic tests、release gates、benchmarks、adapter conformance 和 reproducible-release 等 job；Python/R runtime qualification 在 Ubuntu/macOS 通过，Windows 跳过。

------

# 4. 问题清单

## P0：必须在任何稳定发布前解决

### P0-1：`research_query_corpus` 绕过模型数据外传策略

**代码事实**

项目 manifest 定义了 `modelEgressAllowed`、允许 provider 和允许数据类别；新项目默认值甚至是 `modelEgressAllowed: true`、`sensitivity: public`。参见 [project/init.ts L80–104](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/project/init.ts#L80-L104)。

但 `research_query_corpus` 只执行：

```ts
const project = await options.requireProject(ctx);
const result = await queryCorpusRecords(project.root, params, toolCallId);
return toolResponse(withoutOperation(result));
```

该路径没有把当前 model/provider、project egress policy 或 data class 交给任何授权函数。参见 [tools.ts L4799–4813](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/extension/tools.ts#L4799-L4813)。

`queryCorpusRecords()` 又能返回 source abstract、document blocks、evidence 和 claims；它的核心职责是查询与分页，而不是模型外传授权。参见 [evidence/query.ts L1–340](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/evidence/query.ts#L1-L340)。

**推断**

在标准 Pi tool loop 中，tool result 会成为后续模型上下文。因此，即使用户把项目设为 `modelEgressAllowed=false`，模型仍可调用该工具取得语料。这使“关闭模型外传”不能构成可靠保密边界。

**影响**

- 私密访谈、未发表论文、内部政策材料可能发送给远程 provider；
- policy UI 和 manifest 给用户造成虚假的安全预期；
- local-first 声明仍成立，但 privacy-controlled model use 不成立。

**最小修复**

新增一个统一的 `authorizeModelVisiblePayload()`，在任何包含原文、摘要、证据、稿件、质性材料的 tool response 返回前执行：

1. 读取当前 manifest policy；
2. 确定 payload data classes；
3. 校验当前 provider/model；
4. 当 `modelEgressAllowed=false` 时只允许本地 provider 或返回 `PERMISSION_BLOCKED`；
5. 对不允许的数据类别做确定性删除，禁止依赖提示词要求模型“不要看”。

**精确验证**

新增 table-driven integration test：

- `modelEgressAllowed=false`；
- provider 不在 allowlist；
- abstract、document block、evidence、claim、manuscript 五种 payload；
- 每个 case 都必须返回 `PERMISSION_BLOCKED`；
- tool response 不得包含原文片段；
- provider request capture 中不得出现受限文本；
- 本地 provider allow case 应通过；
- 该测试必须作为 required CI job。

------

### P0-2：模型生成的语义性证据内容可被记录成 `deterministic` 或 `imported`

**代码事实**

模型可见的 `EvidenceDraftSchema` 包含 `paraphrase`、`evidenceStatement`、`claimLinks`，同时允许模型选择：

```ts
method: "deterministic" | "imported"
```

参见 [tools.ts L292–336](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/extension/tools.ts#L292-L336)。

随后 `commitEvidence()` 继承模型传入的方法，却强制写入：

```ts
modelProvider: null,
modelId: null,
promptHash: null
```

参见 [tools.ts L2725–2748](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/extension/tools.ts#L2725-L2748)。

**影响**

exact excerpt 仍受确定性校验，但以下内容的 provenance 会失真：

- 摘录的解释；
- “证据说明”；
- supports/refutes 关系；
- claim-link rationale；
- 模型生成的 claim draft。

这会直接破坏“已定位证据—论断—写作”的可审计性。

**最小修复**

- 从模型可见参数中删除 `extraction.method`；
- 由 host 根据执行来源强制写入 `model_suggested`；
- 写入当前 provider、model ID、prompt/tool schema hash、turn ID；
- 将“确定性 locator/excerpt 校验结果”与“语义解释来源”拆成两个字段；
- `imported` 只允许通过受信任 importer 使用，不能由模型工具参数指定。

**精确验证**

1. 模型调用 `research_commit_evidence` 后，所有 paraphrase/evidence statement/claim rationale 必须带 `model_suggested` provenance；
2. 模型尝试提交 `deterministic` 或 `imported` 必须在 schema 层失败；
3. 纯确定性 parser 创建的 locator/excerpt 可记录独立 deterministic provenance；
4. submission gate 对 provenance 缺失的语义字段必须失败；
5. 旧数据迁移不得静默猜测来源，无法确定时标为 `unknown_legacy`。

------

### P0-3：固定依赖 `pdfjs-dist@5.7.284` 落在已知高危范围，且解析路径未启用公开 workaround

**代码事实**

`pi-research-agent` 将 `pdfjs-dist` 固定为 `5.7.284`。参见 [research-agent/package.json L70–105](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/package.json#L70-L105)。

GitHub Security Advisory `GHSA-hq66-cqwq-w95j / CVE-2026-16633` 将 `>=5.6.83,<6.2.108` 标为受影响，高危，修复版本为 `6.2.108`；风险是打开恶意 PDF 时可能执行任意 JavaScript。参见 [GitHub Advisory GHSA-hq66-cqwq-w95j](https://github.com/advisories/GHSA-hq66-cqwq-w95j)。

PDF parser 使用 `getDocument()`，设置了大小、页数、auto-fetch 和 range 限制，但未显式设置 advisory 给出的 `enableScripting:false` workaround。参见 [pdf-parser.ts L1–180](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/documents/pdf-parser.ts#L1-L180)。

Scheduled npm audit 的最近相关运行 [31162296769](https://github.com/Hilbert-beinghappy/research-agent/actions/runs/31162296769) 在 production vulnerabilities 步骤失败。

**尚未验证**

当前 Node-only text extraction 路径是否能稳定触发 advisory 描述的完整攻击链，本次没有执行恶意 PDF PoC。不得把“依赖受影响”夸张成“本项目已证明可远程利用”。

**影响**

科研 Agent 的典型输入恰好是外部 PDF。即使实际 Node 攻击面比浏览器场景更窄，已知高危生产依赖也足以阻断稳定发布。

**最小修复**

- 升级到 `pdfjs-dist@6.2.108` 或更高兼容版本；
- 同时显式设置 `enableScripting:false`，作为 defense in depth；
- 不直接使用自动 major migration；先建立 PDF parser compatibility fixture；
- 对超大、加密、损坏、脚本型、嵌套对象型 PDF 执行 adversarial tests。

**精确验证**

```bash
npm ci
npm audit --omit=dev --audit-level=moderate
npm run test --workspace packages/research-agent -- pdf
npm run check
```

并要求：

- `GHSA-hq66-cqwq-w95j` 不再出现；
- 现有 text/locator/page/hash fixture 全部一致；
- 恶意脚本 PDF 不启动网络、子进程或文件写入；
- Ubuntu、Windows、macOS 均通过；
- parser 输出变化必须经过 golden diff 审查。

------

### P0-4：固定提交没有完成发布 CI 资格

**代码事实**

通用 `.github/workflows/ci.yml` 的所有主要 job 都使用：

```yaml
if: github.repository == 'earendil-works/pi-mono'
```

因此在 Doro 仓库中被跳过。参见 [ci.yml L1–240](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/.github/workflows/ci.yml#L1-L240)。

仓库另有 focused `.github/workflows/research-agent.yml`，正常情况下会在相关路径 push/PR 上运行三平台矩阵。参见 [research-agent.yml L1–260](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/.github/workflows/research-agent.yml#L1-L260)。

但固定提交的 Actions API 只观察到：

- `CI`：skipped；
- `Publish Model Catalog`：skipped；
- 没有固定提交对应的 Research Agent workflow run。

较早提交 `db0c23e…` 的 Research Agent workflow 确实在三平台成功，但它不能证明 `72e83b5…`。

**影响**

固定提交无法证明：

- build/check 没有被品牌入口修改破坏；
- release scanner 和 package qualification 对当前 tree 通过；
- 当前 lockfile 和 PDF 依赖通过审计；
- tag 对应的源码和测试结果一致。

**最小修复**

- 取消 Doro 仓库通用 CI 的上游仓库硬编码，或建立明确的 Doro required workflow；
- focused workflow 不得只依赖易漏掉的 path filter；
- 将 `npm audit`、release scan、package pack、clean install、SBOM/NOTICE 检查纳入 PR/push required checks；
- tag 发布必须引用已通过全部 required checks 的 SHA。

**精确验证**

在补丁 SHA 上必须出现并成功：

1. Linux/Windows/macOS check；
2. deterministic tests；
3. e2e scenarios；
4. adapter conformance；
5. Python/R runtime qualification；
6. npm production audit；
7. release scan；
8. `npm pack` 后全新目录安装；
9. SBOM/NOTICE/license scan；
10. branch protection API 显示这些 check 为 required。

------

## P1：发布后会造成高维护或数据完整性风险

### P1-1：项目事务没有可见的跨进程 writer lock

`records.ts` 和 `transactions.ts` 使用 expected manifest revision、staging、journal、backup 和最终 manifest commit，能够发现顺序执行下的 stale write；但没有观察到跨进程 lease、lockfile 或 OS file lock。两个进程可能同时在同一 revision 上完成 preflight，随后交错写入。参见 [records.ts L1–500](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/project/records.ts#L1-L500) 和 [transactions.ts L1–520](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/project/transactions.ts#L1-L520)。

`atomic-write.ts` 对临时文件执行 file fsync 后 rename，但未观察到 rename 后对父目录执行 fsync；断电级目录项持久性仍未闭合。参见 [atomic-write.ts L1–120](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/project/atomic-write.ts#L1-L120)。

**修复与验证：** 增加单项目 writer lease、持有者 PID/nonce/expiry、CAS recheck、父目录 fsync；用两个独立 Node 进程对同一项目并发提交 1,000 次，要求无 lost update、无混合 manifest、恢复后 validator 全通过。

------

### P1-2：第三方包和 Doro 启动面没有稳定闭合

- 根 monorepo 版本是 `0.0.3`；
- research package 是 `2.0.0`；
- Pi coding-agent 是 `0.83.0`；
- research 包只有 `research-agent-rpc` bin；
- 常规 Doro 启动仍依赖 Pi 配置和 extension；
- README 说明 npm 包尚未公开；
- 多个 exports 直接指向 `src/*.ts`。

这不会妨碍作者从源码使用，但会使第三方误判产品版本、入口和可支持 API。参见 [package.json L1–140](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/package.json#L1-L140)、[research-agent-rpc.mjs L1–240](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/bin/research-agent-rpc.mjs#L1-L240) 和 [README L140–220](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/README.md#L140-L220)。

**修复与验证：** 发布编译后的 `dist`，冻结 exports map；提供一个薄 `doro` launcher 或明确宣布“Doro 是 Pi profile，不提供独立 CLI”；在空目录执行 `npm pack`、install、init、open、RPC inspect、uninstall。

------

### P1-3：SDK/RPC 是真实实现，但信任和隐私边界只适合本地主机用户

SDK/RPC 只有七个只读方法，这一点设计合理；server 使用 newline-delimited JSON、消息大小限制和 schema validation，也不监听网络。参见 [sdk-rpc.ts L1–520](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent-contracts/src/sdk-rpc.ts#L1-L520) 和 [rpc/server.ts L1–420](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/rpc/server.ts#L1-L420)。

但 SDK project summary 可返回绝对项目根路径，RPC 没有身份认证，release 文档也将其定义为信任本地主机用户。若第三方宿主记录 RPC 返回，可能泄漏用户名和目录结构。

**修复与验证：** 默认返回 project ID 和逻辑 locator；绝对路径需显式 `includeHostPaths` 能力；在日志 snapshot test 中扫描用户名、home path、API key 和原始敏感文本。

------

### P1-4：分析路径是真进程执行，不是安全沙箱

`runtime.ts` 会真实 spawn Python/R/Stata，限制 timeout、stdout/stderr 和导入路径，但子进程仍继承本机用户权限；Linux/Windows 没有通用强沙箱。参见 [runtime.ts L1–680](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/analysis/runtime.ts#L1-L680)。

前序 runtime qualification 真实执行了 Python 和 R；Stata 仅在 `STATA_BIN` 存在时运行。参见 [qualify-v0.3-runtimes.ts L1–360](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/scripts/qualify-v0.3-runtimes.ts#L1-L360)。

**修复与验证：** 文档明确“host-user execution”；默认禁网、最小环境变量、独立临时目录；加入脚本尝试访问 home、SSH key、网络、父目录和 symlink 的负向测试。Stata 只有在真实 licensed runner CI 通过后才能标为 qualified。

------

### P1-5：真实科研工作流测试少于接口和 fixture 测试

Scenario A/D/E 和 integration tests 覆盖了大量状态、故障点和协议形状，质量明显高于普通骨架项目。问题是：

- analysis integration 使用 mock runtime；
- source/citation adapters 多使用 fixture；
- qualitative/case tests 主要是合成记录；
- 没有固定的公开 PDF corpus 和 hostile PDF corpus；
- 没有真实 Zotero/Obsidian host smoke；
- 没有长会话/context-compaction 续跑 benchmark；
- 没有多进程 writer 测试。

例如 [analysis-workbench.test.ts L1–420](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/test/integration/analysis-workbench.test.ts#L1-L420) 中主要通过 mock runtime 生成产物。

**修复与验证：** 增加一组许可明确的小型真实科研 corpus，执行 search/import→parse→evidence→claim→analysis→manuscript→review→restore 全链路，并在网络不可用、provider 失败、parser 失败和中途 kill 后恢复。

------

### P1-6：依赖安全审计是定时任务，不是发布门

`.github/workflows/npm-audit.yml` 只在定时和手动触发时运行，没有 PR/push trigger；audit 失败后 signature audit 也被跳过。参见 [npm-audit.yml L1–80](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/.github/workflows/npm-audit.yml#L1-L80)。

**修复与验证：** 将 production audit 和 signature audit 拆成独立 required steps；PR、main push 和 tag 都必须执行；临时 waiver 必须有 advisory ID、风险解释、期限和 owner。

------

### P1-7：没有形成清晰、可审计的上游 Pi 同步策略

**代码事实：** 实际 Pi 包为 0.83.0，research release 文档提到兼容性资格，但在固定 tree 中没有找到独立的 upstream baseline、patch queue、冲突政策或双版本 CI 文档。

**影响：** 后续同步 Pi 时，Doro 品牌入口、session hooks、工具上下文和 package exports 的冲突可能依赖维护者个人记忆。

**最小修复：**

新增 `UPSTREAM.md`，固定：

- upstream repo、tag、commit；
- Doro patch 集；
- 不允许直接改写的 Pi 区域；
- 每次同步所需的 compatibility matrix；
- 回滚方式；
- Doro-only files 和 shared files 列表。

验证要求是从干净 upstream 0.83.0 或下一目标版本按文档重放 patch，生成的 tree hash 和预期一致。

------

## P2：可以延期，但应进入重构队列

### P2-1：`extension/tools.ts` 过度集中

14 个聚合工具是合理的模型接口设计，但所有 schemas、dispatch、approval、状态编排集中在一个约 175 KB 文件中。它增加审查难度，也正是 egress 和 provenance 漏洞容易出现的原因。

**修复：** 保持对模型暴露的 14 个工具不变，内部拆为每个领域一个 handler module，并强制所有 handler 通过统一 guard/operation wrapper。

------

### P2-2：contracts 提前暴露了未实现 evidence levels

contracts 支持 `table_figure`、`dataset_appendix`，但当前 evidence commit 明确返回 unsupported。参见 [schemas.ts L600–780](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent-contracts/src/schemas.ts#L600-L780) 和 [evidence/commit.ts L250–430](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/evidence/commit.ts#L250-L430)。

**修复：** 不删除 schema enum，避免破坏兼容性；增加 capability negotiation，使第三方明确知道哪些等级在当前 host 中可提交。

------

### P2-3：Zotero、Obsidian 命名容易超过真实实现

Zotero 代码能生成确定性导入格式和导出 profile；Obsidian 主要是 artifact/vault export。参见 [zotero.ts L1–360](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/adapters/export/zotero.ts#L1-L360)。

**修复：** README 使用“Zotero export/push adapter”和“Obsidian-compatible Markdown export”，在真实宿主集成通过前不要写成无条件集成。

------

### P2-4：公共 exports 面过宽

直接导出 `src/*.ts` 会让第三方依赖内部目录和未冻结类型，扩大 semver 责任。

**修复：** 公共包只导出 contracts、SDK、RPC client、adapter protocol 和有限稳定 facade；内部状态实现不公开。

------

# 5. 安全与开源发布审计

| 项目                       | 审计结论                   | 证据与边界                                                   |
| -------------------------- | -------------------------- | ------------------------------------------------------------ |
| **密钥**                   | **PARTIAL PASS**           | release scanner 会扫描 secret、credential、email、绝对路径和敏感配置；但固定提交没有独立执行结果。参见 [scan-release.ts L1–520](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/scripts/scan-release.ts#L1-L520)。 |
| **隐私**                   | **FAIL**                   | 文件状态本地优先，但 `research_query_corpus` 未执行模型 egress policy。 |
| **本地路径泄漏**           | **PARTIAL**                | path normalization 和 symlink escape 防护较强；SDK/RPC 可能暴露绝对 root。参见 [kernel/paths.ts L1–220](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/src/kernel/paths.ts#L1-L220)。 |
| **Prompt injection**       | **PARTIAL / 未闭合**       | exact excerpt 与 canonical state 验证能阻止部分伪造；但没有观察到统一的“检索文本是非指令数据”taint 边界，也没有 corpus-to-model egress gate。 |
| **恶意 PDF**               | **FAIL**                   | 已知高危依赖，未启用公开 workaround，未执行 hostile PDF suite。 |
| **Adapter 恶意输出**       | **较强但非完整**           | JSONL size、timeout、schema、signature、permissions、brokered HTTP 已实现；macOS strong isolation 有资格，Linux/Windows 不构成强沙箱。 |
| **分析脚本**               | **PARTIAL**                | 运行在临时目录且有路径限制，但仍是 host-user process，不是容器或 OS sandbox。 |
| **许可证**                 | **PASS，需保持包边界说明** | 根 Pi monorepo 为 MIT；research package 为 Apache-2.0；research 包内有 LICENSE、NOTICE、THIRD_PARTY_NOTICES。参见 [根 LICENSE L1–30](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/LICENSE#L1-L30)、[research LICENSE](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/LICENSE)、[NOTICE](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/NOTICE)。 |
| **NOTICE**                 | **PASS**                   | Doro/Pi notice 和第三方 notice 均存在。                      |
| **SBOM**                   | **PARTIAL PASS**           | SPDX 文件存在，release scanner 校验 package 与直接 runtime dependencies；漏洞发现仍依赖 lockfile/npm audit。参见 [SBOM.spdx.json L1–320](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/packages/research-agent/SBOM.spdx.json#L1-L320)。 |
| **贡献归属**               | **PARTIAL**                | 根 CONTRIBUTING 和 SECURITY 主要继承上游 Pi，尚未形成 Doro-specific disclosure、ownership、review 和 third-party Skill provenance 规则。参见 [CONTRIBUTING.md L1–240](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/CONTRIBUTING.md#L1-L240) 和 [SECURITY.md L1–260](https://github.com/Hilbert-beinghappy/research-agent/blob/72e83b547d9a236757c9aa88777b1734a00f4401/SECURITY.md#L1-L260)。 |
| **第三方 Skills 可分发性** | **CONDITIONAL PASS**       | 11 个 Skills 位于 Apache-2.0 research package 中，未观察到嵌套冲突许可证；但没有独立的 Skill 来源/改编清单。固定发布前应加入 provenance manifest。 |

------

# 6. 测试与复现审计

## 6.1 本次实际执行或查询的项目

| 项目                                                         | 状态                          | 结果                                                         |
| ------------------------------------------------------------ | ----------------------------- | ------------------------------------------------------------ |
| `git clone https://github.com/Hilbert-beinghappy/research-agent.git` | **BLOCKED**                   | 当前执行环境 DNS 无法解析 `github.com`，未取得本地工作树。   |
| 固定提交存在性与内容读取                                     | **OBSERVED PASS**             | GitHub connector 成功读取 commit、代码、测试、workflow、license 和 Actions 数据。 |
| 本地 Git                                                     | **OBSERVED PASS**             | `git version 2.47.3`。                                       |
| 本地 Node                                                    | **OBSERVED BELOW MINIMUM**    | `v22.16.0`，低于项目要求的 Node `>=22.19.0`；不能作为本地资格环境。 |
| 本地 npm                                                     | **OBSERVED PASS**             | `10.9.2`。                                                   |
| `npm ci`                                                     | **BLOCKED**                   | 没有可用本地工作树。                                         |
| `npm run check`                                              | **BLOCKED**                   | 没有可用本地工作树。                                         |
| research focused tests                                       | **BLOCKED**                   | 没有可用本地工作树。                                         |
| release scan                                                 | **BLOCKED**                   | 没有可用本地工作树。                                         |
| `npm pack` / clean install                                   | **BLOCKED**                   | 没有可用本地工作树。                                         |
| 本地 `npm audit`                                             | **BLOCKED**                   | 没有可用本地工作树。                                         |
| 固定提交通用 CI                                              | **OBSERVED SKIPPED**          | Actions API 显示 CI skipped。                                |
| 固定提交 Research Agent focused CI                           | **NOT OBSERVED**              | 固定 SHA 没有对应 run。原因尚未验证。                        |
| 前序提交三平台 focused CI                                    | **OBSERVED PASS，非固定提交** | Run `31152618515` 成功。                                     |
| scheduled production npm audit                               | **OBSERVED FAIL，较早 SHA**   | Run `31162296769` 在 production vulnerability audit 失败。   |
| Python/R qualification                                       | **OBSERVED PASS，较早 SHA**   | Ubuntu/macOS 前序 CI 成功。                                  |
| Windows Python/R qualification                               | **NOT RUN**                   | workflow 中跳过。                                            |
| Stata qualification                                          | **NOT RUN**                   | 没有 `STATA_BIN` 证据。                                      |
| 恶意 PDF PoC                                                 | **NOT RUN**                   | 不宣称已复现漏洞。                                           |
| 真实 Zotero/Obsidian host                                    | **NOT RUN**                   | 只有代码和 fixture 证据。                                    |
| 多进程并发 writer                                            | **NOT RUN**                   | 并发风险为代码推断。                                         |
| 长会话/context compaction benchmark                          | **NOT RUN**                   | 无法据此宣称长期上下文恢复性能。                             |

## 6.2 对“此前本地通过”的裁决

用户提供的“`npm run check`、科研扩展聚焦测试、发布扫描通过”属于**外部陈述，未被本次本地执行独立复核**。

前序 GitHub Actions 为这些陈述提供了一部分外部佐证，但：

- 对应提交不是固定审计提交；
- npm audit 已出现独立失败；
- 固定提交没有 focused run；
- 因此不能转化成“本提交已通过发布门”。

------

# 7. 首轮整改 Backlog

以下按依赖顺序控制在十个工作日内。

| 顺序 | 任务                                                         | 预计   | 验收标准                                                     |
| ---- | ------------------------------------------------------------ | ------ | ------------------------------------------------------------ |
| 1    | **建立统一 model-visible payload guard**，先覆盖 `research_query_corpus` | 1.5 天 | egress off、provider mismatch、restricted class 测试全部阻断；provider capture 中无受限文本。 |
| 2    | **修复 evidence provenance**                                 | 1.5 天 | 模型不能提交 deterministic/imported；模型语义字段自动带 provider/model/prompt/turn；submission gate 拒绝缺失 provenance。 |
| 3    | **升级 pdfjs-dist 并加 hostile PDF suite**                   | 2 天   | `npm audit` 无该 GHSA；三平台 parser tests 通过；脚本 PDF 无外部副作用。 |
| 4    | **修复 CI 触发与 required checks**                           | 1 天   | 补丁 SHA 在三平台运行 focused CI；audit、release scan、pack/install 均为 required；无 skipped。 |
| 5    | **添加单项目 writer lease 和目录 fsync**                     | 2 天   | 两独立进程 1,000 次并发写无 lost update；随机 kill 后恢复验证通过。 |
| 6    | **发布面闭合**                                               | 1 天   | 版本说明一致；exports 指向 dist；决定并记录 Doro 是 Pi profile 还是独立 launcher；clean install smoke 通过。 |
| 7    | **真实科研工作流与 adversarial smoke**                       | 1.5 天 | 许可明确 corpus 完成 import→PDF→evidence→analysis→manuscript→review→restore；覆盖 provider/parser/adapter failure。 |
| 8    | **发布治理文件**                                             | 0.5 天 | 新增 Doro SECURITY、UPSTREAM、release checklist、Skill provenance；SBOM/NOTICE 更新。 |

## 两周出口门

只有同时满足以下条件，才能把 `v2.0.1` 判为公开稳定版候选：

- P0 四项全部关闭；
- 固定 SHA required CI 全绿；
- production audit 无 high/critical；
- protected privacy regression 全绿；
- evidence provenance regression 全绿；
- 三平台 package clean install 通过；
- 多进程 writer 测试通过；
- hostile PDF suite 通过；
- 真实科研 smoke 通过；
- release tag 指向被验证的同一 SHA。

------

# 8. 暂时不要修改的内容

## 8.1 不要重写 Pi Core 和 Pi Session

当前项目借用 Pi 0.83.0 的会话、模型和终端能力，科研扩展主要位于独立 package。这个边界总体正确，也有利于同步上游。

**重新评估条件：** 只有当统一 egress guard 无法通过现有 extension hook 实现，才审查是否需要在 Pi Core 增加一个极小、通用的 model-visible payload hook；不能直接 fork 一整套 Doro session。

## 8.2 不要改成数据库状态

文件式 canonical state、hash、revision、journal、artifact 和 migration 符合本地优先、可移植和 Git-friendly 的产品定位。

**重新评估条件：** 多进程 lock 和 transaction stress test 仍无法满足一致性要求，或实测项目规模使文件索引超过性能门。

## 8.3 不要推翻 evidence level 契约

metadata、abstract、fulltext_unlocated 和 fulltext_located 的区分是当前架构中最有价值的部分之一。修订应补 provenance 和 capability negotiation，不应降级为一个模糊 `evidence_text` 字段。

## 8.4 不要把 14 个聚合工具拆成几十个模型工具

14 个聚合工具降低了模型选择复杂度。应拆内部 handler，不要扩大模型可见工具面。

## 8.5 不要改写 adapter protocol

adapter protocol 已包含 init、request、response、cancel、shutdown、permissions 和 brokered HTTP。当前问题是资格、沙箱和失败测试，不是协议完全错误。

## 8.6 不要把 SDK/RPC 立即扩成写接口

只读 SDK/RPC 是合理的 v2.0 边界。第三方 mutation 在 writer lock、权限和 provenance 未修复前不应开放。

## 8.7 不要删除分析规格确认门

analysis specification 的显式确认、输入哈希和产物 manifest 是正确设计。应增强 runtime isolation，而不是让模型直接自由运行分析。

## 8.8 不要为了品牌重命名全部 Pi 包

当前 Doro 只需稳定的产品说明、入口和版本映射。全仓库改名会制造巨大的上游同步冲突，收益很低。

------

# 9. 唯一下一步

**立即为 `research_query_corpus` 增加一个失败优先的回归测试：当项目设置 `modelEgressAllowed=false` 时，远程模型调用该工具必须返回 `PERMISSION_BLOCKED`，且 provider request、tool response 和 operation log 中都不得出现任何摘要、全文块、证据或论断文本；随后实现统一的 model-visible payload guard 使测试通过。**

------

# 附录 A：固定快照后的当前候选状态

> **快照边界：** 第 1–9 节仍只裁决
> `main@72e83b547d9a236757c9aa88777b1734a00f4401`；除第 6.1 节本地 Node 最低版本的事实性勘误外，不作追溯改写。
> 本附录仅记录截至 **2026-08-10** 的候选提交
> `c43ad6b1aae750028cd1d89f6c0478fe62280d37`。

## A.1 当前证据矩阵

| 边界 | 当前状态 | 证据与裁决 |
| --- | --- | --- |
| P0-1 模型外传策略 | **已关闭：实现与确定性回归层** | 统一 fail-closed model-visible payload guard 已覆盖工具入口，敏感内容不进入被阻断的 provider request、response 或 operation log。exact-SHA 三平台 deterministic tests 均通过。 |
| P0-2 语义 provenance | **已关闭：实现与确定性回归层** | 模型不能伪造 deterministic/imported/human provenance；host 从持久化调用派生 provider、model、prompt、schema 与 turn，submission gate 拒绝缺失或不合格来源。exact-SHA 三平台 deterministic tests 均通过。 |
| P0-3 PDF 风险 | **部分关闭** | `pdfjs-dist`、无 viewer/scripting surface、JavaScript-action fail-closed 与 hostile-PDF 回归已经整改；但 exact-`c43ad6b1…` 没有 Production vulnerability audit 或 Registry signature audit run，因此发布证据未闭合。现有 defensive fixture 不应称为真实 CVE exploit PoC。 |
| P0-4 固定提交发布资格 | **开放 / NO-GO** | [Research Agent run 31337975629](https://github.com/Hilbert-beinghappy/research-agent/actions/runs/31337975629) 为 exact SHA，最终仅 Windows core 失败；七项 required contexts 中 4 项 success、1 项 failure、2 项 audit missing，不能宣称 required checks 全绿。 |
| 当前上游可重放性 | **已关闭：候选源码层** | Ubuntu 从官方 Pi 固定 commit/tree 直接 fetch，在 fresh repository 中按单父链重放 82/82 patches，结果 tree 与候选精确一致；[artifact 9044924584](https://github.com/Hilbert-beinghappy/research-agent/actions/runs/31337975629/artifacts/9044924584)。 |
| Windows writer lease | **已关闭：当前回归层** | Win32 `readLease()` 对 `EPERM` 仅作每次调用 4×25ms 有界重试；第五次仍原样失败，非 Windows 不重试，不把不可读锁当 stale。exact-SHA Windows deterministic tests 完整通过，1,000-write 双进程路径未再出现 `EPERM`。 |
| Personal Memory 与 compaction | **已关闭：技术回归层** | promotion 独立来源不再被多 locator 虚增；真实 `AgentSession.compact()` 后重新加载 extension，项目链接、system prompt 与 egress policy 恢复。Windows 10k store 有 10,000 笔 committed transactions、六项一致性检查全 true；retrieval raw/E2E P95 为 21.8022/32.0946ms，均通过。 |
| 10-Agent 合成 pilot | **已完成：synthetic technical preflight** | P01–P10 使用独立目录、配置、profile、会话与记忆；20 个 session 引用经 HMAC 假名化，且在本次运行内互异，原始路径、偏好、session ID 与 secret 不进入汇总。该证据始终标记 `synthetic=true`、`betaPilotStarted=false`、`stableStudyEligible=false`。 |
| Ubuntu/macOS 发布包 | **条件通过，不构成三平台发布** | Ubuntu 与 macOS core、macOS transaction、public corpus 均 success；两平台 tarball 逐包一致。Windows 因正式性能门失败，release qualification 与 tarball 均未执行。 |

## A.2 仍然开放的产品与研究边界

### Windows v0.3 qualitative 统一 5 秒门

exact-SHA Windows 正式结果：

- data import 五样本 P95 `361.532ms`，通过；
- qualitative P95 `8832.173ms > 5000ms`，失败；
- failure-only shadow 仍失败，P95 `7499.476ms`；[diagnostic artifact 9045394268](https://github.com/Hilbert-beinghappy/research-agent/actions/runs/31337975629/artifacts/9045394268)。

诊断中 1,001-write transaction 的阶段中位数为：`journal_prepare=3390.788ms`、`staged_verify=286.232ms`、`data_commit=2719.801ms`、`post_commit_verify=271.165ms`。在同时保留逐文件 durability、manifest-last、即时哈希验证与统一 5 秒门时，没有已证实的安全小修可以闭合差距。

必须由产品所有者选择其一：

1. 保留 5 秒门，授权 pack/shard 或 generation-based storage format 迁移；
2. 保留现有逐文件合同，制定有实测余量且版本化的 Windows-specific SLO；
3. 两者都不改，维持 Windows 与整体发布 **NO-GO**。

### 真人与长期研究

10 个记忆隔离的子 Agent 只充当**合成技术参与者**，不替代真人、知情同意、真实使用环境或外部有效性。当前没有 10 名真实 beta 参与者，没有完成 30 人样本，也没有 12 周纵向研究。因此不得据此声称 usability 已验证、beta pilot 已启动、stable-v3 eligible 或真人长期效果成立。

## A.3 发布与可见性读回

截至本附录日期：

- `main` 仍为 `72e83b547d9a236757c9aa88777b1734a00f4401`，候选尚未合并；
- 当前分支没有 PR，仓库没有 tag 或 GitHub Release；
- `pi-research-agent@3.0.0-beta.1` 与 `@research-agent/contracts@2.1.0` 在 npm registry 均为 E404；
- 仓库当前观测为 **PUBLIC**。PUBLIC 仅表示可见，不等于正式发布或通过资格；本附录不授权可见性变更。

**当前总裁决：** `c43ad6b1…` 可以称为实现持续收束中的 technical/source candidate。由于 exact-SHA audits 缺失、Windows 统一 5 秒门失败、Windows release package 未执行且真人/12 周研究证据不存在，当前不得打发布 tag、发布 npm/GitHub Release，或宣传为 beta release、stable release、真人 pilot 已完成或 longitudinally validated。
